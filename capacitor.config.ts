import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.diamentor.app',
  appName: 'Blue Stone',
  webDir: 'dist/digitalCard/browser',
  bundledWebRuntime: false
};

export default config;
